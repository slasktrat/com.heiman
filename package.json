{
  "name": "com.heiman",
  "version": "1.0.0",
  "description": "Adds support for Heiman Smoke Sensor",
  "main": "app.js",
  "dependencies": {
    "homey-meshdriver": "^1.2.28"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
